while (0 <= a) do
    b := b + (2 + (a + (a * 2 - c)) * a);
    d := b;
    while (false or (not(true) or b > a and c <= a or a * 100 >= c)) do
        c := c + 1;

        d := d + 1;
        while (d > 0) do
            c := c * 2;
            d := d - 500;
            count := 2;
            while (count >= 0 ) do
                a := a + 2;
                count := count - 1
            od
        od

    od;
    a := a - 1
od